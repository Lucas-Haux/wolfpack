[query]
length = 10
url = "https://search.nixos.org/backend/latest-42-nixos-24.05/_search"

[nix]
location = "/home/luke/Projects/wolfpack/config.nix"
rebuild_switch = true # ask to rebuild after install
rebuild_command = "sudo nixos-rebuild switch --flake /home/luke/Flake#desktop"
